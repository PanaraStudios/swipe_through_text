name: swipe_through_text
description: SwipeThroughText is a customizable Flutter widget that allows users to swipe through text. The package is suitable for use in various types of apps, like to-do lists, and more.
version: 0.0.4
homepage: https://github.com/PanaraStudios/swipe_through_text

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
